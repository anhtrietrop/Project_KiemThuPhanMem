// Setup chung cho tất cả tests
beforeAll(async () => {
  // Khởi tạo database test, mock services, etc.
  console.log('🧪 Test suite starting...');
});

afterAll(async () => {
  // Dọn dẹp sau khi test xong
  console.log('✅ Test suite completed');
});